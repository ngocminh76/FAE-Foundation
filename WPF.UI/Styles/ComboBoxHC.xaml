<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"                  
                    >
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="ScrollViewer.xaml" />
    </ResourceDictionary.MergedDictionaries>
    <!--  Background  -->
    <SolidColorBrush x:Key="bg-000" Color="Yellow" />
    <SolidColorBrush x:Key="bg-100" Color="Red" />
    <SolidColorBrush x:Key="bg-200" Color="#FF4E596B" />
    <SolidColorBrush x:Key="bg-300" Color="Yellow" />
    <SolidColorBrush x:Key="bg-400" Color="#FF4E596B" />
    <SolidColorBrush x:Key="bg-500" Color="Red" />
    <SolidColorBrush x:Key="bg-600" Color="Blue" />

    <!--  Foreground  -->
    <SolidColorBrush x:Key="fg-000" Color="#f2f2f2" />
    <SolidColorBrush x:Key="fg-100" Color="#e5e5e5" />
    <SolidColorBrush x:Key="fg-200" Color="#d9d9d9" />
    <!--mau khung item-->
    <SolidColorBrush x:Key="fg-300" Color="#FF3B4453" />
    <SolidColorBrush x:Key="fg-400" Color="#bfbfbf" />
    <SolidColorBrush x:Key="fg-500" Color="#b2b2b2" />
    <SolidColorBrush x:Key="fg-600" Color="#a6a6a6" />

    <Style TargetType="Control" x:Key="CommonStyle">
        <Setter Property="Background" Value="{DynamicResource bg-300}" />
        <Setter Property="Foreground" Value="{DynamicResource fg-000}" />
        <Setter Property="BorderBrush" Value="{DynamicResource bg-100}" />
        <Setter Property="FontSize"
                Value="12" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="FontStretch" Value="Normal" />
        <!-- Setter Property="LayoutTransform" Value="{DynamicResource FontStretchExpanded}" / -->
        <Setter Property="FontStyle" Value="Normal" />
    </Style>

    <!--  Primary  -->
    <SolidColorBrush x:Key="primary-lighten-3" Color="#3d9be6" />
    <SolidColorBrush x:Key="primary-lighten-2" Color="#2989e1" />
    <SolidColorBrush x:Key="primary-lighten-1" Color="#FF6B88A5" />
    <SolidColorBrush x:Key="primary-default" Color="#0462d4" />
    <SolidColorBrush x:Key="primary-darken-1" Color="#0152c6" />
    <SolidColorBrush x:Key="primary-darken-2" Color="#0043b8" />
    <SolidColorBrush x:Key="primary-darken-3" Color="#0035a9" />

    <!--  Status  -->
    <SolidColorBrush x:Key="success" Color="#18CB56" />
    <SolidColorBrush x:Key="warning" Color="#CE941B" />
    <SolidColorBrush x:Key="danger" Color="#CB2D2B" />

    <!--  Inverted  -->
    <SolidColorBrush x:Key="inverted-lighten-3" Color="#f2f9ff" />
    <SolidColorBrush x:Key="inverted-lighten-2" Color="#ebf4ff" />
    <SolidColorBrush x:Key="inverted-lighten-1" Color="#e3f0ff" />
    <SolidColorBrush x:Key="inverted-default" Color="#dceaff" />
    <SolidColorBrush x:Key="inverted-darken-1" Color="#d3e1f6" />
    <SolidColorBrush x:Key="inverted-darken-2" Color="#cad9ec" />
    <SolidColorBrush x:Key="inverted-darken-3" Color="#c1d0e3" />


    <ControlTemplate TargetType="ToggleButton" x:Key="ComboBoxToggleButtonTemplate">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20" />
            </Grid.ColumnDefinitions>
            <Border Grid.ColumnSpan="2" Name="Border"
              BorderBrush="{DynamicResource bg-600}" 
              CornerRadius="0"  
              Background="{DynamicResource bg-400}" />


            <Path Name="Arrow" Grid.Column="1" 
            Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z"
            HorizontalAlignment="Left" Fill="{DynamicResource fg-100}"
            VerticalAlignment="Center" />
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
                <Setter Property="Background" TargetName="Border" Value="{DynamicResource primary-lighten-1}"/>
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="True">

                <Setter Property="Shape.Fill" TargetName="Arrow" Value="{DynamicResource fg-000}"/>
            </Trigger>
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="Panel.Background" TargetName="Border" Value="{DynamicResource bg-400}"/>

                <Setter Property="TextElement.Foreground" Value="{DynamicResource bg-500}"/>
                <Setter Property="Shape.Fill" TargetName="Arrow" Value="{DynamicResource bg-400}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="ComboboxHC"  TargetType="{x:Type ComboBox}">
        <Setter Property="UIElement.SnapsToDevicePixels" Value="True"/>
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="TextElement.Foreground" Value="{DynamicResource fg-000}"/>
        <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="FontFamily"
                Value="{DynamicResource SegoeFont}" />
        <Setter Property="FontSize"
                Value="{DynamicResource FontSize12}" />
        <Setter Property="Margin"
                Value="1" />

        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Grid>
                        <ToggleButton Name="ToggleButton" Grid.Column="1"
                                        ClickMode="Press" Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                        Template="{StaticResource ComboBoxToggleButtonTemplate}"/>

                        <ContentPresenter Name="ContentSite"  IsHitTestVisible="False"
                                            HorizontalAlignment="Left" VerticalAlignment="Center"                              
                                            Content="{TemplateBinding ComboBox.SelectionBoxItem}" 
                                            ContentTemplate="{TemplateBinding ComboBox.SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                        <TextBox Name="PART_EditableTextBox"   Margin="5,5,0,0"                   
                                IsReadOnly="{TemplateBinding IsReadOnly}"
                                Visibility="Hidden" Background="Transparent"
                                HorizontalAlignment="Left" VerticalAlignment="Center" Foreground="WhiteSmoke" VerticalContentAlignment="Center"
                                 
                                Focusable="True">
                            <TextBox.Template>
                                <ControlTemplate TargetType="TextBox" >
                                    <Border Name="PART_ContentHost" Focusable="False"  />
                                </ControlTemplate>
                            </TextBox.Template>
                        </TextBox>
                        <!-- Popup showing items -->
                        <Popup Name="Popup" Placement="Bottom"
                                Focusable="False" AllowsTransparency="True"
                                IsOpen="{TemplateBinding ComboBox.IsDropDownOpen}"
                                PopupAnimation="Slide">
                            <Grid Name="DropDown" SnapsToDevicePixels="True"
                                MinWidth="{TemplateBinding FrameworkElement.ActualWidth}"
                                MaxHeight="{TemplateBinding ComboBox.MaxDropDownHeight}">
                                <!--chinh mau comboboxitem-->
                                <Border Name="DropDownBorder" Background="{DynamicResource bg-200}" Margin="0, 1, 0, 0"
                                        CornerRadius="0" BorderThickness="1,1,1,1" 
                                        BorderBrush="{DynamicResource fg-300}"/>
                                <ScrollViewer Margin="1" SnapsToDevicePixels="True" Style="{StaticResource ScrollViewerCAD}">
                                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                </ScrollViewer>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="ItemsControl.HasItems" Value="False">
                            <Setter Property="FrameworkElement.MinHeight" TargetName="DropDownBorder" Value="95"/>
                        </Trigger>
                        <Trigger Property="UIElement.IsEnabled" Value="False">
                            <Setter Property="TextElement.Foreground" Value="{DynamicResource bg-500}"/>
                        </Trigger>
                        <Trigger Property="ItemsControl.IsGrouping" Value="True">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                        </Trigger>
                        <Trigger Property="ComboBox.IsEditable" Value="True">
                            <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
                            <Setter Property="UIElement.Visibility" TargetName="PART_EditableTextBox" Value="Visible"/>
                            <Setter Property="UIElement.Visibility" TargetName="ContentSite" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>