<UserControl x:Class="FAE.Foundation.App.Functions.MongBan.Views.GeometryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FAE.Foundation.App.Functions.MongBan.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WPF.UI;component/Themes/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BrushMauNen}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="File" Style="{StaticResource DgTextBlockLeft}" />
            <TextBox Width="300" Style="{StaticResource TxtLeft}" />
            <Button Content="..." Style="{StaticResource BtnStyleNormal}" />
            <TextBlock Text="Sheet" Style="{StaticResource DgTextBlockLeft}" />
            <ComboBox Width="180" Style="{StaticResource ComboBoxStyle}" />
            <Button Content="Nạp" Style="{StaticResource BtnStyleNormal}" />
        </StackPanel>

        <!-- DataGrid -->
        <Grid Grid.Row="1">
            <DataGrid Style="{StaticResource DataGridStyle}" />
        </Grid>
    </Grid>
</UserControl>
