<UserControl x:Class="FAE.Foundation.App.Functions.MongBan.Views.ForceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FAE.Foundation.App.Functions.MongBan.Views"
             xmlns:vm="clr-namespace:FAE.Foundation.App.Functions.MongBan.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="1800">

    <UserControl.DataContext>
        <vm:ForceViewModel />
    </UserControl.DataContext>

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WPF.UI;component/Themes/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BrushMauNen}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal">
            <TextBlock Text="File"
                       Style="{StaticResource DgTextBlockLeft}" />
            <TextBox Width="300"
                     Style="{StaticResource TxtLeft}"
                     Text="{Binding FilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="True" />
            <Button Content="..."
                    Style="{StaticResource BtnStyleNormal}"
                    Command="{Binding BrowseCommand}" />

            <TextBlock Text="Sheet"
                       Style="{StaticResource DgTextBlockLeft}" />
            <ComboBox Width="180"
                      Style="{StaticResource ComboBoxStyle}"
                      ItemsSource="{Binding Sheets}"
                      SelectedItem="{Binding SelectedSheet, Mode=TwoWay}" />

            <Button Content="Nạp"
                    Style="{StaticResource BtnStyleNormal}"
                    Command="{Binding RunCommand}" />

            <TextBox Style="{StaticResource TxtLeft}"
                     Width="200"
                     Text="{Binding SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock Style="{StaticResource Ari_Block_Base}"
                       Text="{Binding TowersCount, StringFormat='Số lượng: {0}'}"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Bảng hiển thị nội lực sử dụng Style từ WPF.UI library -->
        <Grid Grid.Row="1"
              Margin="4">
            <ListBox Style="{StaticResource ForceListBoxStyle}"
                     ItemsSource="{Binding TowersView}" />
        </Grid>
    </Grid>
</UserControl>
