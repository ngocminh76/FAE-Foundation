<UserControl x:Class="FAE.Foundation.App.Functions.MongBan.Views.ForceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FAE.Foundation.App.Functions.MongBan.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/WPF.UI;component/Themes/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource BrushMauNen}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal">
            <TextBlock Text="Path"
                       Style="{StaticResource DgTextBlockLeft}" />
            <TextBox Width="300"
                     Style="{StaticResource TxtLeft}" />
            <Button Style="{StaticResource BtnStyleNormal}"
                    Content="..." />
            <TextBlock Text="Select sheet"
                       Style="{StaticResource DgTextBlockLeft}" />

            <ComboBox Width="200"
                      Style="{StaticResource ComboBoxStyle}" />

            <Button Style="{StaticResource BtnStyleNormal}"
                    Content="Run" />
        </StackPanel>

        <Grid Grid.Row="1">
            <DataGrid Style="{StaticResource DataGridStyle}">

            </DataGrid>
        </Grid>
    </Grid>
</UserControl>
