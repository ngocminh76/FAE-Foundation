<UserControl x:Class="FAE.Foundation.App.Features.Home.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1200"
             Background="White">

    <!-- Styles for Cards -->
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Margin" Value="20"/>
            <Setter Property="Width" Value="320"/>
            <Setter Property="Height" Value="220"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="15" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="25" ShadowDepth="5"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="40,40,40,20">
            <TextBlock Text="Chọn loại móng cần thiết kế" FontSize="32" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Bộ công cụ thiết kế móng chuyên dụng theo chuẩn TCVN" FontSize="16" Foreground="#7F8C8D" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Cards -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <WrapPanel Margin="20">
                
                <!-- Card 1: Ribbed Raft -->
                <Button Style="{StaticResource CardButtonStyle}" 
                        Command="{Binding DataContext.NavigateProjectCommand, RelativeSource={RelativeSource AncestorType=Window}}">
                    <Border Style="{StaticResource CardStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon Placeholder -->
                            <Border Grid.Row="0" Width="60" Height="60" Background="#E8F4F8" CornerRadius="30" HorizontalAlignment="Left">
                                <TextBlock Text="PRJ" FontSize="20" FontWeight="Bold" Foreground="#2980B9" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                                <TextBlock Text="Dự Án Đường Dây" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                                <TextBlock Text="Quản lý và tính toán hàng loạt móng bè sườn cho dự án có nhiều trụ (Copy từ Excel)." 
                                           FontSize="14" Foreground="#7F8C8D" TextWrapping="Wrap" Margin="0,10,0,0"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2" Text="Sẵn sàng →" Foreground="#2980B9" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </Button>

                <!-- Card 2: Spread Footing (Coming Soon) -->
                <Button Style="{StaticResource CardButtonStyle}" IsEnabled="False" Opacity="0.7">
                    <Border Style="{StaticResource CardStyle}" Background="#F9F9F9">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Width="60" Height="60" Background="#F2F2F2" CornerRadius="30" HorizontalAlignment="Left">
                                <TextBlock Text="MĐ" FontSize="20" FontWeight="Bold" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                                <TextBlock Text="Móng Đơn" FontSize="22" FontWeight="Bold" Foreground="#7F8C8D"/>
                                <TextBlock Text="Tính toán và vẽ cấu tạo móng đơn dưới cột bê tông cốt thép." 
                                           FontSize="14" Foreground="#95A5A6" TextWrapping="Wrap" Margin="0,10,0,0"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2" Text="Sắp ra mắt" Foreground="#E67E22" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </Button>

                <!-- Card 3: Pile Foundation (Coming Soon) -->
                <Button Style="{StaticResource CardButtonStyle}" IsEnabled="False" Opacity="0.7">
                    <Border Style="{StaticResource CardStyle}" Background="#F9F9F9">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <Border Grid.Row="0" Width="60" Height="60" Background="#F2F2F2" CornerRadius="30" HorizontalAlignment="Left">
                                <TextBlock Text="MC" FontSize="20" FontWeight="Bold" Foreground="#95A5A6" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                                <TextBlock Text="Móng Cọc" FontSize="22" FontWeight="Bold" Foreground="#7F8C8D"/>
                                <TextBlock Text="Tính toán đài cọc, sức chịu tải cọc khoan nhồi, cọc ép." 
                                           FontSize="14" Foreground="#95A5A6" TextWrapping="Wrap" Margin="0,10,0,0"/>
                            </StackPanel>

                            <TextBlock Grid.Row="2" Text="Sắp ra mắt" Foreground="#E67E22" FontWeight="Bold" HorizontalAlignment="Right"/>
                        </Grid>
                    </Border>
                </Button>

            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
