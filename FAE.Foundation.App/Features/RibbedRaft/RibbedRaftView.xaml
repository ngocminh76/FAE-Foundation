<UserControl x:Class="FAE.Foundation.App.Features.RibbedRaft.RibbedRaftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:loc="clr-namespace:FAE.Foundation.App.Services.Localization"
             mc:Ignorable="d" 
             Language="en-US"
             d:DesignHeight="700" d:DesignWidth="1200">
    <Grid Background="#F5F6FA">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- LEFT PANEL: INPUTS -->
        <Border Grid.Column="0" Background="White" Margin="10" Padding="15" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="0,0,10,0">
                    <!-- 0. TEN MONG -->
                    <Border Background="#E8F8F5" BorderBrush="#1ABC9C" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Tên Móng:" FontWeight="Bold" Foreground="#16A085" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Model.FoundationName}" FontWeight="Bold" Foreground="#2C3E50" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- 1. THONG SO KICH THUOC -->
                    <Expander Header="Thông số kích thước móng" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="5,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="30"/>
                            </Grid.ColumnDefinitions>
                            
                            <!-- Kich thuoc chan tru Lx -->
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Kích thước chân trụ (Lx):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold" Foreground="#8E44AD"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Model.Lx, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Kich thuoc chan tru Ly -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Kích thước chân trụ (Ly):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold" Foreground="#8E44AD"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Model.Ly, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Kich thuoc mong B -->
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Bề rộng móng (B):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold" Foreground="#2980B9"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Model.TotalWidth, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Kich thuoc mong L -->
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Chiều dài móng (L):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold" Foreground="#2980B9"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Model.TotalLength, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="3" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <!-- Chieu cao mong H -->
                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Chiều cao móng (H):" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Model.Depth, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="4" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <!-- Chieu sau chon mong -->
                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Chiều sâu chôn móng:" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold" Foreground="#D35400"/>
                            <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Model.EmbedmentDepth, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="5" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <!-- Dam mong a -->
                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Bề rộng dầm móng (a):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Model.RibWidth, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="6" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Dam mong h -->
                            <TextBlock Grid.Row="7" Grid.Column="0" Text="Chiều cao dầm móng (h):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Model.RibHeight, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="7" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <!-- Ban mong h1 -->
                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Chiều dày bản móng (h1):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding Model.SlabThickness, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="8" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Ban mong c -->
                            <TextBlock Grid.Row="9" Grid.Column="0" Text="Kích thước đục lỗ (c):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="9" Grid.Column="1" Text="{Binding Model.HoleSize, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="9" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <!-- Co mong b1 -->
                            <TextBlock Grid.Row="10" Grid.Column="0" Text="Bề rộng cổ móng (b1):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="10" Grid.Column="1" Text="{Binding Model.B1, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="10" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                            
                            <!-- Co mong b2 -->
                            <TextBlock Grid.Row="11" Grid.Column="0" Text="Chiều dài cổ móng (b2):" VerticalAlignment="Center" Margin="0,0,0,8" FontWeight="Bold"/>
                            <TextBox Grid.Row="11" Grid.Column="1" Text="{Binding Model.B2, UpdateSourceTrigger=PropertyChanged, Delay=800}" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="11" Grid.Column="2" Text="m" VerticalAlignment="Center" Margin="5,0,0,8"/>
                        </Grid>
                    </Expander>
                    <!-- 2. NOI LUC TINH TOAN -->
                    <Expander Header="Nội lực tính toán" IsExpanded="True" Margin="0,0,0,10">
                        <StackPanel Margin="5,10,0,0">
                            <!-- Base Dimension -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <TextBlock Text="Khoảng cách 2 chân trụ:" FontWeight="Bold" Foreground="#8E44AD" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <TextBox Text="{Binding Model.TowerBaseDimension, UpdateSourceTrigger=PropertyChanged}" Width="80" VerticalAlignment="Center" TextAlignment="Right"/>
                                <TextBlock Text="mm" VerticalAlignment="Center" Margin="5,0,0,0"/>
                            </StackPanel>
                            <!-- DataGrid for Load Cases -->
                            <DataGrid ItemsSource="{Binding AvailableLoadCases}" AutoGenerateColumns="False" CanUserAddRows="True" CanUserDeleteRows="True" HeadersVisibility="Column" HorizontalGridLinesBrush="#E5E7EB" VerticalGridLinesBrush="#E5E7EB" BorderBrush="#BDC3C7" BorderThickness="1" MinHeight="150" Background="White">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Tên tổ hợp" Binding="{Binding Name}" Width="130" FontWeight="SemiBold"/>
                                    <DataGridTextColumn Header="Qx" Binding="{Binding Qx}" Width="60"/>
                                    <DataGridTextColumn Header="Qy" Binding="{Binding Qy}" Width="60"/>
                                    <DataGridTextColumn Header="N" Binding="{Binding N}" Width="60"/>
                                    <DataGridTextColumn Header="Mx" Binding="{Binding Mx}" Width="60"/>
                                    <DataGridTextColumn Header="My" Binding="{Binding My}" Width="60"/>
                                    <DataGridTextColumn Header="Mz" Binding="{Binding Mz}" Width="60"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Expander>

                    <!-- KET QUA TINH TOAN -->
                    <Expander Header="Kết quả tính toán" IsExpanded="True" Margin="0,0,0,10">
                        <Grid Margin="5,10,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Diện tích đáy móng (F):" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Model.FoundationArea, StringFormat=F2}" FontWeight="Bold" Foreground="#16A085" VerticalAlignment="Center" Margin="0,0,0,8" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="m²" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Mômen kháng đáy móng Wx:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Model.Wx, StringFormat=F2}" FontWeight="Bold" Foreground="#E67E22" VerticalAlignment="Center" Margin="0,0,0,8" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Row="1" Grid.Column="2" Text="m³" VerticalAlignment="Center" Margin="5,0,0,8"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Mômen kháng đáy móng Wy:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Model.Wy, StringFormat=F2}" FontWeight="Bold" Foreground="#E67E22" VerticalAlignment="Center" Margin="0,0,0,8" HorizontalAlignment="Right"/>
                            <TextBlock Grid.Row="2" Grid.Column="2" Text="m³" VerticalAlignment="Center" Margin="5,0,0,8"/>
                        </Grid>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- SPLITTER -->
        <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="#BDC3C7"/>

        <!-- RIGHT PANEL: VIEWPORTS -->
        <Grid Grid.Column="2" Background="#E5E8E8" Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- LEFT: PLAN VIEW -->
            <Border Grid.Column="0" Background="White" CornerRadius="5" Padding="5" Margin="2">
                <Grid>
                    <TextBlock Text="{Binding Path=Strings[PlanView], Source={x:Static loc:LocalizationService.Instance}}" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5" Panel.ZIndex="1"/>
                    <Canvas x:Name="PlanCanvas" ClipToBounds="True" Background="#F8FAFC" SizeChanged="DrawingCanvas_SizeChanged"/>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- RIGHT: 3D VIEWPORT -->
            <Border Grid.Column="2" Background="White" CornerRadius="5" Padding="5" Margin="2" MouseLeftButtonDown="Viewport3D_MouseLeftButtonDown" MouseLeftButtonUp="Viewport3D_MouseLeftButtonUp" MouseMove="Viewport3D_MouseMove" ClipToBounds="True">
                <Grid>
                    <TextBlock Text="{Binding Path=Strings[View3D], Source={x:Static loc:LocalizationService.Instance}}" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="5" Panel.ZIndex="1"/>
                    <Viewport3D x:Name="MainViewport3D">
                        <Viewport3D.Camera>
                            <PerspectiveCamera x:Name="Camera3D" Position="15,18,22" LookDirection="-15,-18,-22" UpDirection="0,1,0" FieldOfView="45"/>
                        </Viewport3D.Camera>
                        <ModelVisual3D>
                            <ModelVisual3D.Content>
                                <Model3DGroup x:Name="Model3DGroup">
                                    <Model3DGroup.Transform>
                                        <Transform3DGroup>
                                            <RotateTransform3D>
                                                <RotateTransform3D.Rotation>
                                                    <AxisAngleRotation3D x:Name="RotX" Axis="1,0,0" Angle="0"/>
                                                </RotateTransform3D.Rotation>
                                            </RotateTransform3D>
                                            <RotateTransform3D>
                                                <RotateTransform3D.Rotation>
                                                    <AxisAngleRotation3D x:Name="RotY" Axis="0,1,0" Angle="0"/>
                                                </RotateTransform3D.Rotation>
                                            </RotateTransform3D>
                                        </Transform3DGroup>
                                    </Model3DGroup.Transform>
                                </Model3DGroup>
                            </ModelVisual3D.Content>
                        </ModelVisual3D>
                    </Viewport3D>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
